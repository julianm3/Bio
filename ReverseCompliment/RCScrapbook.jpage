String str = new String("CATG");		
String result = new String("");
		for (int i = str.length() -1 ;i >= 0; i--){
			result.concat(str.substring(i,i+1));
		}
System.out.println("Result is: " + result);